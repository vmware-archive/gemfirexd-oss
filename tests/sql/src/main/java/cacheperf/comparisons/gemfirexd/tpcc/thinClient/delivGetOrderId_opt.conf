include $JTESTS/cacheperf/comparisons/gemfirexd/tpcc/thinClient/delivGetOrderId.conf;

hydra.Prms-testDescription = "SELECT no_o_id FROM new_order WHERE no_d_id = ? AND no_w_id = ? ORDER BY no_o_id ASC FETCH FIRST ROW ONLY";

cacheperf.comparisons.gemfirexd.tpcc.TPCCPrms-optimizeDelivGetOrderId = true;
