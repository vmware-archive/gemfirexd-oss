hydra.HostPrms-hostNames = monster;
hydra.HostPrms-resourceDirBaseMapFileName = $JTESTS/bin/scaleperf/dirmap.prop;

//------------------------------------------------------------------------------
// PERSISTENCE

// GFE
hydra.RegionPrms-dataPolicy      = persistent_partition  partition;
hydra.RegionPrms-diskStoreName   = disk                  none;
hydra.RegionPrms-diskSynchronous = true                  default;

hydra.DiskStorePrms-names = disk;

hydra.Prms-removeDiskFilesAfterTest = true;

// GFXD

hydra.gemfirexd.GfxdHelperPrms-persistDD = true;
hydra.gemfirexd.GfxdHelperPrms-persistTables = true;
