cacheperf.comparisons.gemfirexd.QueryPerfPrms-mySQLServerHost = <host3>;

hydra.HostPrms-hostNames =
  fcn "hydra.TestConfigFcns.pool(\"<host1>\", ${locatorHosts})" ncf
  fcn "hydra.TestConfigFcns.pool(\"<host2>\", ${clientHosts})" ncf
  fcn "hydra.TestConfigFcns.pool(\"<host3>\", ${serverHosts})" ncf
  ;
