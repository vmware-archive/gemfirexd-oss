sql.SQLPrms-isOffheap = false;       // do not allow all tables to be off-heap
sql.SQLPrms-randomizeOffHeap = true; // off-heap keyword on create table statement randomized, some tables heap, some tables off-heap

