sql/dataextractor/p2pDataExtract.conf
    locatorHosts = 1 locatorVMsPerHost=2 locatorThreadsPerVM = 1
    A=peer peerHosts=1 peerVMsPerHost=4 peerThreadsPerVM=2
    B=controller controllerHosts=1 controllerVMsPerHost=1 controllerThreadsPerVM=1
    tableDefaultPartitioned=false
    testPartitionBy=true
    performDDLOps=false
    redundantCopies=1
    performUpdatesWhileShuttingDown = true
    simultaneousShutdownVMs = false
    securities=trade.securities:replicate:persistent
    customers=trade.customers:replicate:persistent
    networth=trade.networth:replicate:persistent
    portfolio=trade.portfolio:replicate:persistent
    sellorders=trade.sellorders:replicate:persistent
    buyorders=trade.buyorders:replicate:persistent
    txhistory=trade.txhistory:replicate:persistent
    employees=emp.employees:replicate:persistent
    trades=trade.trades:replicate:persistent
    product=sqlfire

sql/dataextractor/p2pDataExtract.conf
    locatorHosts = 1 locatorVMsPerHost=2 locatorThreadsPerVM = 1
    A=peer peerHosts=1 peerVMsPerHost=4 peerThreadsPerVM=2
    B=controller controllerHosts=1 controllerVMsPerHost=1 controllerThreadsPerVM=1
    tableDefaultPartitioned=false
    testPartitionBy=true
    performDDLOps=true
    redundantCopies=1
    performUpdatesWhileShuttingDown = true
    simultaneousShutdownVMs = false
    securities=trade.securities:replicate:persistent
    customers=trade.customers:replicate:persistent
    networth=trade.networth:replicate:persistent
    portfolio=trade.portfolio:replicate:persistent
    sellorders=trade.sellorders:replicate:persistent
    buyorders=trade.buyorders:replicate:persistent
    txhistory=trade.txhistory:replicate:persistent
    employees=emp.employees:replicate:persistent
    trades=trade.trades:replicate:persistent
    product=sqlfire

sql/dataextractor/p2pDataExtract.conf
    locatorHosts = 1 locatorVMsPerHost=2 locatorThreadsPerVM = 1
    A=peer peerHosts=1 peerVMsPerHost=4 peerThreadsPerVM=2
    B=controller controllerHosts=1 controllerVMsPerHost=1 controllerThreadsPerVM=1
    tableDefaultPartitioned=false
    testPartitionBy=true
    performDDLOps=false
    redundantCopies=1
    performUpdatesWhileShuttingDown = false
    simultaneousShutdownVMs = true
    securities=trade.securities:replicate:persistent
    customers=trade.customers:replicate:persistent
    networth=trade.networth:replicate:persistent
    portfolio=trade.portfolio:replicate:persistent
    sellorders=trade.sellorders:replicate:persistent
    buyorders=trade.buyorders:replicate:persistent
    txhistory=trade.txhistory:replicate:persistent
    employees=emp.employees:replicate:persistent
    trades=trade.trades:replicate:persistent
    product=sqlfire

sql/dataextractor/p2pDataExtract.conf
    locatorHosts = 1 locatorVMsPerHost=2 locatorThreadsPerVM = 1
    A=peer peerHosts=1 peerVMsPerHost=4 peerThreadsPerVM=2
    B=controller controllerHosts=1 controllerVMsPerHost=1 controllerThreadsPerVM=1
    tableDefaultPartitioned=false
    testPartitionBy=true
    performDDLOps=true
    redundantCopies=1
    performUpdatesWhileShuttingDown = false
    simultaneousShutdownVMs = true
    securities=trade.securities:replicate:persistent
    customers=trade.customers:replicate:persistent
    networth=trade.networth:replicate:persistent
    portfolio=trade.portfolio:replicate:persistent
    sellorders=trade.sellorders:replicate:persistent
    buyorders=trade.buyorders:replicate:persistent
    txhistory=trade.txhistory:replicate:persistent
    employees=emp.employees:replicate:persistent
    trades=trade.trades:replicate:persistent
    product=sqlfire

sql/dataextractor/p2pDataExtractCorrupt.conf
    locatorHosts = 1 locatorVMsPerHost=2 locatorThreadsPerVM = 1
    A=peer peerHosts=1 peerVMsPerHost=4 peerThreadsPerVM=2
    B=controller controllerHosts=1 controllerVMsPerHost=1 controllerThreadsPerVM=1
    tableDefaultPartitioned=false
    testPartitionBy=true
    performDDLOps=false
    redundantCopies=1
    performUpdatesWhileShuttingDown = true
    simultaneousShutdownVMs = false
    securities=trade.securities:replicate:persistent
    customers=trade.customers:replicate:persistent
    networth=trade.networth:replicate:persistent
    portfolio=trade.portfolio:replicate:persistent
    sellorders=trade.sellorders:replicate:persistent
    buyorders=trade.buyorders:replicate:persistent
    txhistory=trade.txhistory:replicate:persistent
    employees=emp.employees:replicate:persistent
    trades=trade.trades:replicate:persistent
    product=sqlfire

sql/dataextractor/p2pDataExtractCorrupt.conf
    locatorHosts = 1 locatorVMsPerHost=2 locatorThreadsPerVM = 1
    A=peer peerHosts=1 peerVMsPerHost=4 peerThreadsPerVM=2
    B=controller controllerHosts=1 controllerVMsPerHost=1 controllerThreadsPerVM=1
    tableDefaultPartitioned=false
    testPartitionBy=true
    performDDLOps=true
    redundantCopies=1
    performUpdatesWhileShuttingDown = true
    simultaneousShutdownVMs = false
    securities=trade.securities:replicate:persistent
    customers=trade.customers:replicate:persistent
    networth=trade.networth:replicate:persistent
    portfolio=trade.portfolio:replicate:persistent
    sellorders=trade.sellorders:replicate:persistent
    buyorders=trade.buyorders:replicate:persistent
    txhistory=trade.txhistory:replicate:persistent
    employees=emp.employees:replicate:persistent
    trades=trade.trades:replicate:persistent
    product=sqlfire

    
    
