hydra.GemFirePrms-offHeapMemorySize=0m;               // this sets off-heap memory for older tests (ignored in newer tests)
hydra.gemfirexd.FabricServerPrms-offHeapMemorySize=0m;  // this sets off-heap memory for newer tests (ignored in older tests)
sql.SQLPrms-isOffheap = false;                           // true means all tables are offheap (via offheap keyword on create table statement)
