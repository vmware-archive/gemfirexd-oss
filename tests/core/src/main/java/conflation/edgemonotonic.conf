include $JTESTS/conflation/edgeconflation.conf;

hydra.Prms-testDescription = "Make sure conflation does not lose or duplicate values, where the data feeds are edge clients instead of server peers.";

conflation.ConflationPrms-feedMonotonically = true;
conflation.ConflationPrms-validateMonotonic = true;
