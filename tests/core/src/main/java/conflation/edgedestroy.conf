include $JTESTS/conflation/edgemonotonic.conf;

hydra.Prms-testDescription = "Do occasional destroys between monotonic updates, where the data feeds are edge clients instead of server peers.";

conflation.ConflationPrms-destroyPercentage = 25;
