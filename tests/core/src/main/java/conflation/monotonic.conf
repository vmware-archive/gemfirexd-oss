include $JTESTS/conflation/conflation.conf;

hydra.Prms-testDescription = "Make sure conflation does not lose or duplicate values.";

conflation.ConflationPrms-feedMonotonically = true;
conflation.ConflationPrms-validateMonotonic = true;
