hydra.HostPrms-hostNames = <host1> <host2>;
hydra.HostPrms-osTypes = windows;
hydra.HostPrms-bootstrapPort = <port>;
hydra.HostPrms-javaHomes = J:\where\jdk\1.6.0_26\x86.Windows_NT;

//For distFuncScale2.bt to run on Windows, we need to specify the JDBC path.
hydra.VmPrms-extraClassPaths 	+= 	j:/where/jdbcdriver/oracle/jdbc/lib/classes12.jar;
