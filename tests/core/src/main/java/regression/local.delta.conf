//its testing person responsibly to provide 5.7 version build location (current location is for linux)
//For WDC network we use the following, it works for other networks too.
hydra.VersionPrms-gemfireHome = default /export/gcm/where/gemfire/releases/GemFire5.7-all/product;

//This is prevoius setting, it does not work for WDC
//hydra.VersionPrms-gemfireHome = default /gcm/where/gemfire/57/snaps.Any/snapshots/product;


