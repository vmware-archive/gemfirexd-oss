hydra.Prms-testDescription = " This test is for testing eviction threshold notification delivery with PR TX.  ";

INCLUDE $JTESTS/resman/evictionThreshold.conf;

// enable transactions (supported in ParRegTest.doEntryOperations()
getInitialImage.InitImagePrms-useTransactions = true;

