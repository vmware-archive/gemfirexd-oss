include $JTESTS/hdfs/parReg/concParRegHAPersist.conf;

hydra.ClientPrms-versionNames = 
      fcn "hydra.TestConfigFcns.pool(\"version1 default\", ${${A}Hosts})" ncf 
      fcn "hydra.TestConfigFcns.pool(\"version1 default\", ${${B}Hosts})" ncf 
      ;

hydra.VersionPrms-names   = version1;
hydra.VersionPrms-version = ${version1};


