#===============================================================================
#  TEST CONFIGURATION VARIABLES
#===============================================================================

  hydra.numHosts = 2
  # see hydraconfig/systemparamsN.inc (number of hosts)

  vmQuantities = 2
  # see hydra.ClientPrms-vmQuantities

  vmThreads = 1
  # see hydra.ClientPrms-vmThreads

  scope = global

  trimSeconds = 150
  # see cacheperf.CachePerfPrms-trimSeconds

  workSeconds = 210
  # see cacheperf.CachePerfPrms-workSeconds

  unlockAfterLock = false
  # see dlock.DLSPerPrms-unlockAfterLock
