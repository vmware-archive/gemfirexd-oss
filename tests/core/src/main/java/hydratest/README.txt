
This directory contains hydra configuration files for tests of hydra itself.
Some tests are expected to fail.  In most cases, the logs must be examined
to determine that the test functioned as expected (hydra does not have a
self-validation layer).

