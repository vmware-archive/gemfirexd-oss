// Dispatches some meaningless tasks without using a GemFire system.

INCLUDE $JTESTS/hydraconfig/hydraparams1.inc;

TASK taskClass = hydratest.TaskClient   taskMethod = tryItOutRobinG
     ;

hydra.Prms-totalTaskTimeSec = 10;

hydra.ClientPrms-vmQuantities = 4;
hydra.ClientPrms-vmThreads = 5;

hydratest.HydraTestPrms-prm01 = robing 1 2 3 4 5 gnibor;
