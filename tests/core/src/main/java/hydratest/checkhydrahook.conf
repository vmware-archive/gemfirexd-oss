// Tests task that encounters unexpected exception.  The test should pass and skip the shutdown hook.

include $JTESTS/hydratest/checkhydra.conf;

hydra.Prms-clientShutdownHook = hydratest.TaskClient shutdownHook;
