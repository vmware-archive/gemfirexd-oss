//
// Use this local.conf to run client2 using a private X.XX build.
//
hydra.ClientPrms-versions = default version;

hydra.VersionPrms-names = version;
hydra.VersionPrms-version = XXX;
hydra.VersionPrms-gemfireHome = <path_to_private_build>;
// default for XXX is /export/gcm/where/gemfire/releases/GemFireX.XX-all/product
