// Tests task that encounters unexpected exception.  The test should fail and execute a shutdown hook.

include $JTESTS/hydratest/checkhydraerror.conf;

hydra.Prms-clientShutdownHook = hydratest.TaskClient shutdownHook;
