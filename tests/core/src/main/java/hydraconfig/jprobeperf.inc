// JPROBE!!! Include this, then set your JPROBE_HOME , run execute-hydra-test or execute-battery

hydra.ClientPrms-jprobeNames = clientProbe;

hydra.JProbePrms-names = clientProbe;
hydra.JProbePrms-function = performance;
hydra.JProbePrms-filters = *;