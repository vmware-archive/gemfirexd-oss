// gemfireprms.inc - allow ability to globally set GemFirePrms for all 
// tests and then force the test developer to override these values that
// require a specific setting on a per test basis
hydra.GemFirePrms-conserveSockets = true;
