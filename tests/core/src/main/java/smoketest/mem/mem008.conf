include $JTESTS/smoketest/mem/mem001.conf;
hydra.Prms-testDescription = "Data is put into the cache and the size of the cache is measured. Entry time to live is enabled but no entries are actually expired. Uses normal data policy and local scope";
hydra.RegionPrms-entryTimeToLive = 600 invalidate ;
