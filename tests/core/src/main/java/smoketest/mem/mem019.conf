include $JTESTS/smoketest/mem/mem016.conf;
hydra.Prms-testDescription = "Data is put into the cache and the size of the cache is measured. Creates a functional index on a list with 5 values which are all unique across all entries. Uses normal data policy and local scope.";
