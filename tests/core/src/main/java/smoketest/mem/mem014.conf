include $JTESTS/smoketest/mem/mem001.conf;
hydra.Prms-testDescription = "Data is put into the cache and the size of the cache is measured. Uses a partitioned region";
hydra.RegionPrms-partitionName = putter;
hydra.PartitionPrms-names = putter;
hydra.PartitionPrms-totalNumBuckets = ${totalNumBuckets};
hydra.PartitionPrms-redundantCopies = ${redundantCopies};

