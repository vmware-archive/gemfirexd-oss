include $JTESTS/smoketest/mem/mem009.conf;
hydra.Prms-testDescription = "Data is put into the cache and the size of the cache is measured. Entry eviction with overflow to disk is enabled. All entries are evicted. Uses normal data policy and local scope";
