include $JTESTS/smoketest/mem/mem002.conf;
hydra.Prms-testDescription = "Has two VMS in the same DS. Data is put into the cache from one VM. The cache size is measured in both VMs. Uses replicate data policy and global scope";
