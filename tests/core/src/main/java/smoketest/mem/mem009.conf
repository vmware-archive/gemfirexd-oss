include $JTESTS/smoketest/mem/mem001.conf;
hydra.Prms-testDescription = "Data is put into the cache and the size of the cache is measured. Entry eviction with local destroy is enabled but no entries are actually evicted. Uses normal data policy and local scope";
hydra.RegionPrms-evictionAttributes = lruEntryCount ${entryCount} ${action} ;
