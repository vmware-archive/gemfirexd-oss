include $JTESTS/smoketest/mem/mem016.conf;
hydra.Prms-testDescription = "Data is put into the cache and the size of the cache is measured. Creates a functional index on a a single field which has 500 possible values. Uses normal data policy and local scope.";
