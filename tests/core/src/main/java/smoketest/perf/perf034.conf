include $JTESTS/smoketest/perf/perf028.conf;

hydra.Prms-testDescription = "Updater updates persistent entries using LRU memory size with action overflowToDisk.  Entries are byte arrays of size ${dataSize}.";

hydra.RegionPrms-dataPolicy = persistentReplicate;
