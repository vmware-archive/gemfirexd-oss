include $JTESTS/smoketest/perf/perf031.conf;

hydra.Prms-testDescription = "Updater updates persistent entries.  Entries are Serializable NestedOrderVectors with width ${dataWidth} and depth ${dataDepth}.";

cacheperf.CachePerfPrms-objectType = objects.NestedOrderVector;
objects.NestedOrderVectorPrms-width = ${dataWidth};
objects.NestedOrderVectorPrms-depth = ${dataDepth};
objects.ArrayOfBytePrms-size = unused;
