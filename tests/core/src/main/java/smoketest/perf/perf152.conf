include $JTESTS/smoketest/perf/perf017.conf;

hydra.Prms-testDescription = "Empty edge clients do 80% fetches and and 20% updates on entries from partitioned servers.  Runs on two hosts.  Entries are DataSerializable PSTObjects with byte arrays of size ${dataSize}.";

hydra.RegionPrms-dataPolicy    = partition  empty;
