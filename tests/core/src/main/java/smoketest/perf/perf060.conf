include $JTESTS/smoketest/perf/perf056.conf;
include $JTESTS/hydraconfig/topology_p2p_2.inc;

hydra.Prms-testDescription = "Empty publishers update entries in partitioned datastore.  Conserve sockets is ${conserveSockets}.  Entries are DataSerializable PSTObjects with byte arrays of size ${dataSize}.  Runs on two hosts.";
