include $JTESTS/smoketest/perf/perf077.conf;

hydra.Prms-testDescription = "Empty edge clients update entries in which they have registered interest on server.  Entries are DataSerializable PSTObjects with byte arrays of size ${dataSize}.";

cacheperf.CachePerfPrms-objectType = objects.PSTObject;
objects.PSTObjectPrms-size         = ${dataSize};
objects.ArrayOfBytePrms-size       = unused;
