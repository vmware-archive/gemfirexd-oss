hydra.Prms-testRequirement = "Test region operations on many replicate regions that share more than one disk store. Some regions
are persistent, some overflow to disk; useUniqueKeys = false";
hydra.Prms-testDescription = "
";

INCLUDE $JTESTS/diskRecovery/multiDiskStorePR.conf;
diskRecovery.RecoveryPrms-useUniqueKeys = false;


