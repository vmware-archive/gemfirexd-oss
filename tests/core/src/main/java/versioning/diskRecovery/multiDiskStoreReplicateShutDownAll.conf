hydra.Prms-testRequirement = "Test region operations on many persistent replicate regions with shutDownAllMembers; although shutDownAllMembers is really for persistent PRs, this is here to be sure it does not affect non-PR persistent regions; useUniqueKeys=false";
hydra.Prms-testDescription = "
";

INCLUDE $JTESTS/diskRecovery/multiDiskStoreReplicateShutDownAll.conf;
diskRecovery.RecoveryPrms-useUniqueKeys = false;

