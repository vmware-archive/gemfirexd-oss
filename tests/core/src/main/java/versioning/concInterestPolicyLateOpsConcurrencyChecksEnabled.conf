hydra.Prms-testRequirement = "Allow multiple writers in replicate bridgeServers without causing data consistency issues";
hydra.Prms-testDescription = "concInterestPolicy.conf with concurrencyChecksEnabled (versioning) and useOwnKeys=false";

INCLUDE $JTESTS/hct/concInterestPolicyLateOps.conf;

hydra.RegionPrms-concurrencyChecksEnabled = true;

