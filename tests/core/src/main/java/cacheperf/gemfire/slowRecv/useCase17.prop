A = pub
pubHosts = 1
pubVMsPerHost = 1
pubThreadsPerVM = 1

B = sub
subHosts = 1
subVMsPerHost = 1
subThreadsPerVM = 1

# conserveSockets
conserveSockets = false 

# scope (distributedAck, distributedNoAck, global)
scope = distributedNoAck

# characters in message payload string (this is half the byte size)
messageSize = 0

# whether to sleep before updating cache entries
sleepBeforeOp = false

# how many operations to do per sleep
sleepOpCount = 1

# low end of random sleep range
putSleepMsMin = 5

# high end of random sleep range
putSleepMsMax = 5
