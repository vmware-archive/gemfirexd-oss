include $JTESTS/cacheperf/gemfire/messaging/gets.inc;

hydra.Prms-testDescription = "Two caches work on a common region while remaining caches (${numExtra}) contain different regions.  All caches store their regions in VM memory";

distcache.gemfire.GemFireCachePrms-regionName = 
      ROBING r1 r2 r3 r4 r5 r6 r7 r8 r9 r10 GNIBOR;

