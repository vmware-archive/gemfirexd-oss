hydra.HostPrms-hostNames =
 fcn
  "hydra.TestConfigFcns.pool(\"localhost\", ${locatorHosts})"
 ncf
 fcn
  "hydra.TestConfigFcns.pool(\"bensg bensh bensi bensk\", ${bridgeHosts})"
 ncf
 fcn
  "hydra.TestConfigFcns.pool(\"bensl bensm\", ${edgeHosts})"
 ncf
 ;
hydra.HostPrms-resourceDirBaseMapFileName = /home/lises/bin/hostmap.prop;

hydra.timeserver.TimeServerPrms-clockSkewThresholdMs = 1000;
hydra.timeserver.TimeServerPrms-clockSkewUpdateFrequencyMs = 5000;
hydra.timeserver.TimeServerPrms-errorOnExceededClockSkewThreshold = true;
