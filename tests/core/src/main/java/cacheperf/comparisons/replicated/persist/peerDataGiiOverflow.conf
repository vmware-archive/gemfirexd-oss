hydra.Prms-testDescription = "Peer datahosts to load into persistent replicate regions with overflow then add a new vm causing it gii";

include $JTESTS/cacheperf/comparisons/replicated/persist/peerDataGii.conf;

hydra.RegionPrms-evictionAttributes = lruEntryCount 100 overflowToDisk;

