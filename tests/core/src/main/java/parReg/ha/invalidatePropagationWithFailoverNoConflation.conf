INCLUDE $JTESTS/hct/ha/invalidatePropagationWithFailoverNoConflation.conf;

//hydra.RegionPrms-names            = bridge      feed       edge;
hydra.RegionPrms-scope              = default     default    default;
hydra.RegionPrms-dataPolicy         = partition   partition  default;
hydra.RegionPrms-partitionName      = bridge      feed       none;
hydra.PartitionPrms-names           = bridge      feed;
hydra.PartitionPrms-redundantCopies = 1;

