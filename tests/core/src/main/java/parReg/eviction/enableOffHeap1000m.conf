hydra.GemFirePrms-offHeapMemorySize=1000m;
hydra.RegionPrms-enableOffHeapMemory = true;

// reset heap memory downward since this test is using off-heap
hydra.VmPrms-extraVMArgs += "-Xmx350m -Xms350m";

