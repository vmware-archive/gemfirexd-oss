include $JTESTS/parReg/newWan/parallel/conflation/wanconflationHA.conf;

hydra.Prms-testDescription = "Test creates partition region with serial gateway sender with batchConflationEnabled. Also bounce the GatewaySender VM. Validate that the sender has conflated events correctly.";

//---------------------------------------------------------------------------------

hydra.GatewaySenderPrms-names     = sender;
hydra.GatewaySenderPrms-parallel  = false ; 
