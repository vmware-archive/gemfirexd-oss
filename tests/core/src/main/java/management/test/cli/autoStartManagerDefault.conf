hydra.Prms-testRequirement = "Automatically start manager(s) in a default system (jmxManager* properties are not explicitly set).";
hydra.Prms-testDescription = "
Start locator, accessor, dataStore and admin members. Connect CLI clients with --locator. Verify manager jvms.
";

INCLUDE $JTESTS/management/test/cli/autoStartManager.inc;

