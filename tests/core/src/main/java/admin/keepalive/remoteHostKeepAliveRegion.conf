INCLUDE $JTESTS/admin/keepalive/keepAliveRegion.conf;

admin.AdminPrms-cacheServerHost = ${host};
admin.AdminPrms-cacheServerRemoteCommand = "ssh -n {HOST} {CMD}";
// use local.conf to change to rsh if needed
